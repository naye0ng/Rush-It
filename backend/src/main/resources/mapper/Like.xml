<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="like">
	<insert id="insertLike" parameterType="Like">
		INSERT
		INTO	like(toilet_id, user_id, like)
		VALUES(#{toilet_id}, #{user_id}, #{like})
	</insert>
	
	<delete id="deleteLike" parameterType="Like">
		DELETE
		FROM	like
		WHERE	toilet_id=#{toilet_id} AND user_id={user_id}
	</delete>
</mapper>

<!-- 
<mapper namespace="answer">
	<insert id="insertAnswer" parameterType="Answer">
		insert into answer(answer_number, answer, qna_number, user_id)
		values(#{answer_number}, #{answer}, #{qna_number}, #{user_id})
	</insert>
	
	<delete id="deleteAnswerList" parameterType="int">
		delete from answer where qna_number=#{qna_number}
	</delete>
	
	<delete id="deleteAnswer" parameterType="int">
		delete from answer where answer_number=#{answer_number}
	</delete>

	<select id="selectAnswerList" resultMap="AnswerResultMap" parameterType="int">
		select answer_number, answer, qna_number, user_id
		from answer
		where qna_number=#{number}
	</select>

	<resultMap type="Answer" id="AnswerResultMap">
		<id column="answer_number" property="answer_number"/>
		<result column="answer" property="answer" />
		<result column="qna_number" property="qna_number" />
		<result column="user_id" property="user_id"/>
	</resultMap>
	 
</mapper> 
 -->