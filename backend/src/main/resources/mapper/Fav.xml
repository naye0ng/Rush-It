<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="fav">

	<select id="selectFav" parameterType="Fav">
		SELECT	*
		FROM	fav
		WHERE	toilet_id=#{toilet_id} AND user_id=#{user_id}
	</select>
	
	<select id="amountOfFav" parameterType="String">
		SELECT	COUNT(*)
		FROM	fav
		WHERE	toilet_id=#{toilet_id} AND fav = true
	</select>
	
	<select id="amountOfNotFav" parameterType="String">
		SELECT	COUNT(*)
		FROM	fav
		WHERE	toilet_id=#{toilet_id} AND fav = false
	</select>
	
	<resultMap type="Love" id="LoveResultMap">
		<result column="toilet_id" property="toilet_id" />
		<result column="user_id" property="user_id" />
		<result column="love" property="love"/>
	</resultMap>
	
	<insert id="insertFav" parameterType="Fav">
		INSERT
		INTO	fav(toilet_id, user_id, love)
		VALUES(#{toilet_id}, #{user_id}, #{love})
	</insert>
	
	<delete id="deleteFav" parameterType="Fav">
		DELETE
		FROM	fav
		WHERE	toilet_id=#{toilet_id} AND user_id={user_id}
	</delete>
</mapper>