<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
	PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="review">
	<insert id="insertReview" parameterType="Review">
		insert into review(id, toilet_id, user_id, review, rating, timestamp)
		values(#{id},#{toilet_id},#{user_id},#{review},#{rating},#{timestamp})
	</insert>
	
	<select id="selectReview" parameterType="int">
		select id, toilet_id, user_id, review, rating, timestamp
		from review
		where id = #{id}
	</select>
	
	<select id="selectReviewList" resultMap="ReviewResultMap" parameterType="String">
		select id, toilet_id, user_id, review, rating, timestamp
		from review
		where toilet_id like "%{toilet_id}%"
	</select>
	
	<delete id="deleteReview" parameterType="int">
		delete from review where id={id}
	</delete>
	
	<resultMap type="Review" id="ReviewResultMap">
		<id column="id" property="id"/>
		<result column="toilet_id" property="toilet_id"/>
		<result column="user_id" property="user_id"/>
		<result column="review" property="review"/>
		<result column="rating" property="rating"/>
		<result column="timestamp" property="timestamp"/>
	</resultMap>
	
</mapper>