export default {
    user_loc(state) {
        return state.map.user_loc;
    },
    draw_map(state) {
        return state.map.draw_map;
    },
    draw_marker(state) {

    },
    get_place_list(state) {
        return state.map.place_list;
    }
}